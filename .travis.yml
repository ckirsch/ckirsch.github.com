sudo: required
os: osx
before_install: brew update && brew install brew-cask && brew cask install mactex
script: make realclean && make all && make clean
deploy:
  provider: pages
  skip_cleanup: true
notifications:
  email: false